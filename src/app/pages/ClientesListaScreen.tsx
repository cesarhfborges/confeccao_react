import * as React from 'react';
import {SafeAreaView, View} from 'react-native';
import {Text} from 'react-native-paper';

const ClientesLista = () => (
  <SafeAreaView>
    <View>
      <Text>ClientesLista</Text>
    </View>
  </SafeAreaView>
);

export default ClientesLista;
